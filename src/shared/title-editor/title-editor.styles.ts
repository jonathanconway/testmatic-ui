import styled from "styled-components";

export const Input = styled.input`
  font-size: inherit;
  font-family: inherit;
  font-weight: inherit;
  height: 100%;
  border: none;
  margin: 0;
  padding: 0.25rem;
  flex: 1;
`;
