import { buttonBase } from "../button";
import styled from "styled-components";

export const Button = styled.button`
  ${buttonBase}

  padding: 0.125rem;
  width: 1.25rem;
  height: 1.25rem;
  line-height: 0.9rem;
`;
